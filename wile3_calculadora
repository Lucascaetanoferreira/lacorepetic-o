#esse codigo começa com o while true que é verdadeiro cria as variaveis como opcão tipo escolher operação atraves
dos numero  ae vem match  opçao que vai ser pelo P. entra no cases pede a variavel da operação cria um laço de repetição
e coloca uma uma variavel e digite um numero e faz o calculo eo p torna a parada e tem break tanbem. e a divisão não pode ser 
0.

while True:
    opcao = input('Digite a opcao 1 - Adição \n2 - Subtração\n3 - Multiplicação\n4 - Divisão\nX - Sair\nOpcao: ')
    match opcao.upper():
        case '1':
            soma = 0
            while True:
                num = input('Digite um numero')
                soma = soma  + float(num)
                if num.upper() == 'P':
                    break
            print(f"A soma é {soma}")
        case 'X':
            break
        case '2':
            adicao = 0
            while True:
                num= input('digete um numero')
                adicao = adicao - float(num)
                if num.upper() == 'P':
                   break
            print(f"A adição é {adicao}")
            break
        case '3':
            multiplicação = 0
            while True:
                num= input('digete um numero')
                multiplicação = multiplicação * float(num)
                if num.upper() == 'P':
                   break
            print(f"A multiplicação é {multiplicação}")
            break
        case '4':
           divisao = 0
           while True:
                num= input('digete um numero')
                divisao = divisao / float(num)
                if num.upper() == 'P':
                   break
                if divisao ==0.0:
                    continue
                


             
